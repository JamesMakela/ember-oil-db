<div class="container">
  <div class="row">
    <div class="col-sm-4 col-lg-2">
      <strong>ID:</strong> {{oil.id}}
      <br>

      <strong>Type:</strong>
      {{#if oil.productType}}
        {{oil.productType}}
      {{else}}
        None
      {{/if}}
      <br>

      {{#if oil.status}}
        <ul>
          {{#each oil.status as |stat|}}
            <li><i class="status-bad">{{stat}}</i></li>
          {{/each}}
        </ul>
      {{else}}
        <strong>
          Status: <i class="status-good">OK</i>
        </strong>
      {{/if}}
    </div>

    <div class="col-sm-6 col-lg-4">
      <strong>Location:</strong>
      {{#if oil.location}}
        {{oil.location}}
      {{else}}
        None
      {{/if}}
      <br>

      <strong>Reference:</strong>
      {{#if oil.reference}}
        {{oil.reference}}
      {{else}}
        None
      {{/if}}
      <br>

      <strong>Reference Date:</strong>
      {{#if oil.referenceDate}}
        {{! Europe/London is how we lookup GMT in moment.js }}
        {{moment-format (unix oil.referenceDate) "YYYY-MM-DD" timeZone="Europe/London"}}
      {{else}}
        None
      {{/if}}
      <br>

      <strong>Comments:</strong>
      {{#if oil.comments}}
        {{oil.comments}}
      {{else}}
        None
      {{/if}}
      <br>
    </div>  

    <div class="col-sm-6 col-lg-4">
      <strong>API:</strong>

      {{#each oil.apis as |api|}}
        <span class="badge badge-dark">
          {{round-relative api.gravity 2}}
        </span>
      {{else}}
        <span>
          No Values
        </span>
      {{/each}}
      <br>

      <strong>Categories:</strong>

      {{#each oil.categories as |category|}}
        <span class="badge badge-dark">
          {{category}}
        </span>
      {{else}}
        None
      {{/each}}
    </div>
  </div>

  <div class="row">

    <div class="col-sm">
    </div>

  </div>

  <div class="row">
    <div class="col-sm">
    </div>
  </div>

  <div class="row">
    <div class="col-sm">
    </div>
  </div>

</div>
